/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package group16.project16;

/**
 *  InstanceFactory is a Factory for User instances (a login session). Sets up
 * Board, List, and Card objects for the User that is logged in.
 * 
 * @author ryan
 */
public class InstanceFactory {
/*    
    public User getUser() {
        String uName = User.loginSequence();        // verifies User credentials and returns the User's name is legitimate
        if (uName.equals("Invalid_Login")) {       
            return null;                            // return null if the User did not log in correctly
        }
        else {
            // login is valid, finish building instance
            User thisUser = new User(uName, uPass); // we don't have pass... do we need it beyond login?
            thisUser.loadData();    // attempt to load data for that user
        }
        
    }
*/
    
    
}
